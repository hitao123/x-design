import Tooltip from './Tooltip.vue';
import { withInstall } from '@x-design/utils';

export const XTooltip = withInstall(Tooltip);
export default XTooltip;

export type { TooltipProps } from './types';
